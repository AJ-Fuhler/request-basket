import { basketModel } from '../models/basketModel';

export const basketController = {
  handleGetBaskets() {
    // 
  },
  
  handleRedirectToBaskets() {
    // 
  },

  handleGetBasketRequests(endpoint: string) {
    //
  },

  handleCreateNewBasket(requestedEndpoint: string) {
    //
  },

  handleClearBasket(endpoint: string) {
    //
  },
}
